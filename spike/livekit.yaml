port: 7880
log_level: info
rtc:
  tcp_port: 7881
  use_external_ip: false
  ips:
    includes:
      - 100.97.118.89/32   # Tailscale — web + local agent
      - 192.168.1.0/24     # LAN — phone (react-native-webrtc doesn't enumerate Tailscale interface)
  enable_loopback_candidate: true   # local agent peer needs to reach itself
keys:
  devkey: secret
